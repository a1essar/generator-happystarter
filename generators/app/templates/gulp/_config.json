{
    "autoprefixerOptions": [
        "ie >= 9",
        "ie_mob >= 10",
        "ff >= 30",
        "chrome >= 34",
        "safari >= 7",
        "opera >= 23",
        "ios >= 7",
        "android >= 4.4",
        "bb >= 10"
    ],
    "paths": {<% _.forEach(paths, function(value, key) { %>
        "<%= key %>": "<%= value %>"<%if(key !== Object.keys(paths)[Object.keys(paths).length - 1]){%>,<%}%><% }); %>
    }
}
